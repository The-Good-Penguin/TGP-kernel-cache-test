header:
  version: 12
  includes:
    - kas/base.yml
machine: qemux86-64
local_conf_header:
  10_base: |
    hostname:pn-base-files = "qemutest"
    IMAGE_FSTYPES:append = " wic.qcow2"
  99_grub: |
    GRUB_BOOT_IMAGE = "grub-efi"
    WKS_FILE = "qemu-grub.wks"
    
